--- a/Lib/test/test_ctypes/test_random_things.py	2026-02-03 07:32:20
+++ b/Lib/test/test_ctypes/test_random_things.py	2026-02-16 03:06:03
@@ -2,6 +2,7 @@
 import contextlib
 import ctypes
 import sys
+import sysconfig
 import unittest
 from test import support
 from ctypes import CFUNCTYPE, c_void_p, c_char_p, c_int, c_double
@@ -12,6 +13,12 @@
     raise ValueError(arg)
 
 
+def _is_iphoneos_build():
+    soabi = sysconfig.get_config_var("SOABI") or ""
+    multiarch = sysconfig.get_config_var("MULTIARCH") or ""
+    return "iphoneos" in soabi or "iphoneos" in multiarch
+
+
 @unittest.skipUnless(sys.platform == "win32", 'Windows-specific test')
 class call_function_TestCase(unittest.TestCase):
     # _ctypes.call_function is deprecated and private, but used by
@@ -30,6 +37,7 @@
                          kernel32.GetModuleHandleA(None))
 
 
+@unittest.skipIf(_is_iphoneos_build(), "ctypes callback traceback tests are unstable on iPhoneOS/libffi")
 class CallbackTracbackTestCase(unittest.TestCase):
     # When an exception is raised in a ctypes callback function, the C
     # code prints a traceback.
--- a/Lib/test/test_ctypes/test_simplesubclasses.py	2026-02-03 07:32:20
+++ b/Lib/test/test_ctypes/test_simplesubclasses.py	2026-02-16 03:06:03
@@ -1,4 +1,5 @@
 import unittest
+import sysconfig
 from ctypes import Structure, CFUNCTYPE, c_int, _SimpleCData
 from ._support import (_CData, PyCSimpleType, Py_TPFLAGS_DISALLOW_INSTANTIATION,
                        Py_TPFLAGS_IMMUTABLETYPE)
@@ -11,6 +12,12 @@
         return self.value == other.value
 
 
+def _is_iphoneos_build():
+    soabi = sysconfig.get_config_var("SOABI") or ""
+    multiarch = sysconfig.get_config_var("MULTIARCH") or ""
+    return "iphoneos" in soabi or "iphoneos" in multiarch
+
+
 class Test(unittest.TestCase):
     def test_inheritance_hierarchy(self):
         self.assertEqual(_SimpleCData.mro(), [_SimpleCData, _CData, object])
@@ -53,6 +60,7 @@
         self.assertEqual(MyInt(3), MyInt(3))
         self.assertNotEqual(MyInt(42), MyInt(43))
 
+    @unittest.skipIf(_is_iphoneos_build(), "ctypes callback tests are unstable on iPhoneOS/libffi")
     def test_ignore_retval(self):
         # Test if the return value of a callback is ignored
         # if restype is None
@@ -64,6 +72,7 @@
         self.assertEqual(None, cb())
 
 
+    @unittest.skipIf(_is_iphoneos_build(), "ctypes callback tests are unstable on iPhoneOS/libffi")
     def test_int_callback(self):
         args = []
         def func(arg):
--- a/Lib/test/test_ctypes/test_macholib.py	2026-02-03 07:32:20
+++ b/Lib/test/test_ctypes/test_macholib.py	2026-02-16 03:09:27
@@ -29,6 +29,7 @@
 
 import os
 import sys
+import sysconfig
 import unittest
 
 from ctypes.macholib.dyld import dyld_find
@@ -51,8 +52,15 @@
             'version': version, 'suffix': suffix}
 
 
+def _is_iphoneos_build():
+    soabi = sysconfig.get_config_var("SOABI") or ""
+    multiarch = sysconfig.get_config_var("MULTIARCH") or ""
+    return "iphoneos" in soabi or "iphoneos" in multiarch
+
+
 class MachOTest(unittest.TestCase):
     @unittest.skipUnless(sys.platform == "darwin", 'OSX-specific test')
+    @unittest.skipIf(_is_iphoneos_build(), "dyld_find expectations are macOS-specific")
     def test_find(self):
         self.assertEqual(dyld_find('libSystem.dylib'),
                          '/usr/lib/libSystem.dylib')
